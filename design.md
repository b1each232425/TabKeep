# AI Design Reference

更新日期：2026-06-30

这个文件用于沉淀不能直接作为 MCP 使用的设计资源，并规定 AI 在做 UI/UX 工作时如何使用它们。项目级设计规范仍以各项目自己的 `DESIGN.md` 为最高优先级。

## 当前状态

### Magic UI

- 类型：React / Tailwind / Motion 组件库。
- MCP：已安装。
- 用法：作为组件上下文和实现参考，适合营销页、官网、空状态、欢迎页、展示型区块。
- 注意：不要直接套默认视觉；必须改造成当前项目的设计系统。

### UI-UX Pro Max

- 类型：AI design skill，不是 MCP。
- Codex skill：已安装到用户级 Codex skills，重启 Codex 后生效。
- 用法：用于生成设计系统、检查 UI/UX 质量、选择布局/色彩/字体/动效原则。
- 注意：它是通用设计智能库，不能覆盖项目已有品牌和设计约束。

### React Bits

- 类型：React 动效组件库，不是 MCP。
- MCP：未发现官方 MCP。
- 用法：只作为微交互、文字动效、局部背景效果参考。
- 注意：不要安装 npm 上名为 `react-bits` 的包；那不是 reactbits.dev 对应的项目。

## AI 使用优先级

1. 先读当前项目的 `DESIGN.md`。
2. 再用 UI-UX Pro Max 做设计判断：层级、布局、颜色、字体、动效、可访问性。
3. 需要现成组件上下文时，用 Magic UI MCP 查组件。
4. 需要更有表现力的局部动效时，参考 React Bits 源码或官网示例。
5. 最后必须把所有外部组件改回当前项目的视觉语言。


## 推荐提示词

```text
你是当前项目的产品设计工程师。
先读取项目 DESIGN.md，再参考 UI-UX Pro Max 的设计规则。
如果需要组件上下文，可以使用 Magic UI MCP；如果需要局部动效灵感，可以参考 React Bits。
不要直接套默认样式。必须适配当前项目的颜色、字体、间距、圆角、密度和动效规则。
输出前检查：可访问性、响应式、文本溢出、视觉层级、交互状态、是否有 AI 常见炫技感。
```

## 交付前检查

- 是否遵守项目 `DESIGN.md`。
- 是否有明确的信息层级，而不是堆卡片。
- 是否保留足够的留白和对齐纪律。
- 是否给按钮、输入、菜单、标签、状态提供完整交互态。
- 是否避免装饰性动效和无意义背景。
- 是否支持键盘导航、焦点态、低动效偏好。
- 是否在桌面和移动尺寸下都不重叠、不溢出。

